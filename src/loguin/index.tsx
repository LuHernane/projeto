import { useState } from "react";

export default function Home() {
  const [user, setUser] = useState("");
  const [password, setPassword] = useState("");
  const userId = "redmi";
  const passwordId = "note";

  function Loguin() {
    if (userId === "redmi" && passwordId === "note") {
    }
  }

  return null;
}
